# Система Управління Аптекою

Повнофункціональна система управління онлайн аптекою з React фронтендом, Node.js сервером та Firebase базою даних.

## 📋 Зміст

- [Особливості](#особливості)
- [Технології](#технології)
- [Встановлення](#встановлення)
- [Налаштування](#налаштування)
- [Запуск](#запуск)
- [Структура проекту](#структура-проекту)
- [Функціонал](#функціонал)

## ✨ Особливості

- 🔐 Автентифікація користувачів через Firebase
- 💊 Каталог медикаментів з фільтрацією та пошуком
- 🛒 Кошик покупок з можливістю оформлення замовлення
- 📦 Система управління замовленнями
- 👤 Профіль користувача
- 👨‍💼 Адміністративна панель
- 📱 Адаптивний дизайн
- 🎨 Сучасний медичний UI/UX

## 🛠 Технології

### Backend

- Node.js
- Express.js
- Firebase Admin SDK
- Express Validator

### Frontend

- React 18
- React Router v6
- Firebase Authentication
- Axios
- React Icons
- React Toastify

### База даних

- Firebase Firestore

## 📦 Встановлення

### Вимоги

- Node.js (версія 16 або вище)
- npm або yarn
- Обліковий запис Firebase

### Кроки встановлення

1. **Клонування репозиторію**

```bash
cd "d:\Kyrsovi_na_prodash\other 11"
```

2. **Встановлення залежностей**

```bash
# Встановлення залежностей для всього проекту
npm run install-all
```

Або окремо для кожної частини:

```bash
# Кореневі залежності
npm install

# Серверні залежності
cd server
npm install

# Клієнтські залежності
cd ../client
npm install
```

## ⚙️ Налаштування

### 1. Налаштування Firebase

1. Створіть проект у [Firebase Console](https://console.firebase.google.com/)
2. Увімкніть Authentication (Email/Password)
3. Створіть Firestore Database
4. Згенеруйте приватний ключ для Service Account

### 2. Налаштування сервера

Створіть файл `server/.env` на основі `server/.env.example`:

````env
FIREBASE_PROJECT_ID=ваш-project-id
### 2. Налаштування сервера

Сервер використовує Firebase Admin SDK та потребує Service Account (отримується з Firebase Console → Project Settings → Service Accounts → Generate new private key). Створіть файл `server/.env` на основі `server/.env.example` і додайте ці значення.

Приклад `server/.env` (буквальна вставка приватного ключа має бути спеціально ескейпнута):

```env
FIREBASE_PROJECT_ID=apteka-ee589
FIREBASE_CLIENT_EMAIL=your-service-account@apteka-ee589.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
FIREBASE_DATABASE_URL=https://apteka-ee589.firebaseio.com

PORT=5000
NODE_ENV=development
````

ВАЖЛИВО: `FIREBASE_PRIVATE_KEY` має мати `\n` у місцях переходів на новий рядок. Не заливайте приватний ключ у відкриті репозиторії.

Альтернативні варіанти (більш безпечні):

- **GOOGLE_APPLICATION_CREDENTIALS**: збережіть JSON файл і вкажіть в середовищі шлях до нього:

  ```powershell
  $env:GOOGLE_APPLICATION_CREDENTIALS = "C:\path\to\serviceAccountKey.json"
  ```

  У такому випадку `firebase-admin` автоматично використає applicationDefault credentials.

- **FIREBASE_SERVICE_ACCOUNT_BASE64**: закодуйте JSON у base64 і вставте у `.env` (корисно для CI/CD):

  ```powershell
  $b64 = [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((Get-Content serviceAccountKey.json -Raw)))
  # Установіть FIREBASE_SERVICE_ACCOUNT_BASE64=$b64
  ```

- **FIREBASE_SERVICE_ACCOUNT_JSON**: вставте весь JSON (строку) у `.env` - менш зручно, ніж base64, але можливо для простих розгортань.
  FIREBASE_CLIENT_EMAIL=ваш-client-email
  FIREBASE_PRIVATE_KEY="ваш-private-key"
  FIREBASE_DATABASE_URL=https://ваш-project.firebaseio.com

PORT=5000
NODE_ENV=development

````

### 3. Налаштування клієнта

Відредагуйте `client/src/config/firebase.js`:

```javascript
const firebaseConfig = {
### 3. Налаштування клієнта

Клієнт використовує environment variables у файлі `.env` (приклад `client/.env.example`). Створіть файл `client/.env` на основі `client/.env.example` та вставте ваші значення.

Приклад `client/.env` (взято з наданого конфігу):

```env
REACT_APP_FIREBASE_API_KEY=AIzaSyDCM_z0tscVKDceAssUVupBuGkzA6Tg2Zg
REACT_APP_FIREBASE_AUTH_DOMAIN=apteka-ee589.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=apteka-ee589
REACT_APP_FIREBASE_STORAGE_BUCKET=apteka-ee589.firebasestorage.app
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=1066526075734
REACT_APP_FIREBASE_APP_ID=1:1066526075734:web:2635f3b6bb1fb5cb93c4f7
REACT_APP_API_URL=http://localhost:5000/api
````

Після цього `client/src/config/firebase.js` завантажує налаштування з `process.env.REACT_APP_*` та автоматично ініціалізує Firebase у клієнті.
apiKey: "YOUR_API_KEY",
authDomain: "YOUR_AUTH_DOMAIN",
projectId: "YOUR_PROJECT_ID",
storageBucket: "YOUR_STORAGE_BUCKET",
messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
appId: "YOUR_APP_ID",
};

````

## 🚀 Запуск

### Режим розробки

Запуск сервера та клієнта одночасно:

```bash
npm run dev
````

Або окремо:

```bash
# Запуск сервера (порт 5000)
npm run server

# Запуск клієнта (порт 3000)
npm run client
```

### Продакшн

```bash
# Збірка клієнта
cd client
npm run build

# Запуск сервера
cd ../server
npm start
```

## 🧪 Заповнення тестових даних (seed)

Скрипт seed додає для тестування приклад користувачів, медикаментів і тестове замовлення у Firestore та створює користувачів у Firebase Auth.

1. Перед запуском переконайтесь, що у вас є дійсний `server/.env` або встановлений `GOOGLE_APPLICATION_CREDENTIALS` для service account.

2. Запуск seed:

```powershell
cd server
npm run seed
```

3. Якщо ви хочете видалити попередні дані у базі (users, medicines, orders) і заповнити з нуля, використайте `--force`:

```powershell
npm run seed -- --force
```

⚠️ Увага: Скрипт створює та змінює дані у Firestore і Firebase Auth. Виконуйте лише у тестовому проєкті або власному середовищі.

Примітка: Якщо Cloud Firestore API не ввімкнено, seed буде виконувати лише операції в Firebase Auth (створення/видалення користувачів) та пропускати операції з Firestore (додавання медикаментів/замовлень). Щоб seed повністю заповнив базу (Firestore), увімкніть Firestore API в Google Cloud Console.

ВАЖЛИВО: При використанні `--force` скрипт **окрім очищення Firestore колекцій також видаляє тестових користувачів у Firebase Auth** (user emails, зазначені у `seed`), тому не використовуйте `--force` на реальних проєктах.

### Тестові облікові записи, які створює seed

- Адміністратор: `admin@apteka.local` / пароль: `Admin123!` (роль: admin)
- Тестові користувачі: `ivan@apteka.local`, `olena@apteka.local` / пароль: `User12345!`

З міркувань безпеки, при запуску у спільній інфраструктурі, змініть паролі або видаліть тестові акаунти після використання.

## 📁 Структура проекту

```
apteka-system/
├── server/                    # Backend
│   ├── config/               # Конфігурація
│   │   └── firebase.js       # Налаштування Firebase
│   ├── middleware/           # Middleware
│   │   └── auth.js          # Автентифікація
│   ├── routes/              # API маршрути
│   │   ├── medicines.js     # Маршрути медикаментів
│   │   ├── orders.js        # Маршрути замовлень
│   │   ├── users.js         # Маршрути користувачів
│   │   └── auth.js          # Маршрути автентифікації
│   ├── .env.example         # Приклад змінних оточення
│   ├── package.json
│   └── server.js            # Точка входу
│
├── client/                   # Frontend
│   ├── public/
│   │   └── index.html
│   ├── src/
│   │   ├── components/      # React компоненти
│   │   │   ├── Header/
│   │   │   └── Footer/
│   │   ├── context/         # Context API
│   │   │   ├── AuthContext.js
│   │   │   └── CartContext.js
│   │   ├── pages/           # Сторінки
│   │   │   ├── HomePage/
│   │   │   ├── MedicinesPage/
│   │   │   ├── CartPage/
│   │   │   ├── OrdersPage/
│   │   │   ├── ProfilePage/
│   │   │   ├── LoginPage/
│   │   │   ├── RegisterPage/
│   │   │   └── AdminPage/
│   │   ├── services/        # API сервіси
│   │   │   ├── api.js
│   │   │   └── firebase.js
│   │   ├── config/          # Конфігурація
│   │   │   └── firebase.js
│   │   ├── App.js           # Головний компонент
│   │   ├── index.js         # Точка входу
│   │   └── index.css        # Глобальні стилі
│   └── package.json
│
├── .gitignore
├── package.json
└── README.md
```

## 🎯 Функціонал

### Для користувачів

#### Автентифікація

- Реєстрація нового акаунту
- Вхід в систему
- Вихід з системи
- Управління профілем

#### Каталог медикаментів

- Перегляд всіх медикаментів
- Фільтрація за категоріями
- Пошук за назвою
- Фільтр "В наявності"
- Детальна інформація про кожен медикамент

#### Кошик та замовлення

- Додавання товарів до кошика
- Зміна кількості товарів
- Видалення товарів
- Оформлення замовлення
- Перегляд історії замовлень
- Скасування замовлення (якщо статус "В очікуванні")

### Для адміністраторів

#### Управління медикаментами

- Додавання нових медикаментів
- Редагування існуючих
- Видалення медикаментів
- Управління наявністю

#### Управління замовленнями

- Перегляд всіх замовлень
- Зміна статусу замовлення
- Перегляд деталей замовлення

#### Управління користувачами

- Перегляд списку користувачів
- Зміна ролей користувачів

## 🔑 API Endpoints

### Автентифікація

- `POST /api/auth/register` - Реєстрація
- `POST /api/auth/verify-token` - Перевірка токену

### Медикаменти

- `GET /api/medicines` - Отримання всіх медикаментів
- `GET /api/medicines/:id` - Отримання медикаменту за ID
- `POST /api/medicines` - Створення медикаменту (admin)
- `PUT /api/medicines/:id` - Оновлення медикаменту (admin)
- `DELETE /api/medicines/:id` - Видалення медикаменту (admin)
- `GET /api/medicines/categories/list` - Отримання категорій

### Замовлення

- `GET /api/orders` - Отримання замовлень
- `GET /api/orders/:id` - Отримання замовлення за ID
- `POST /api/orders` - Створення замовлення
- `PUT /api/orders/:id/status` - Оновлення статусу (admin)
- `DELETE /api/orders/:id` - Скасування замовлення

### Користувачі

- `GET /api/users/profile` - Отримання профілю
- `PUT /api/users/profile` - Оновлення профілю
- `GET /api/users` - Отримання всіх користувачів (admin)
- `PUT /api/users/:id/role` - Зміна ролі (admin)

## 🎨 Дизайн

Система використовує сучасний медичний дизайн з:

- Зелено-бірюзовою палітрою кольорів
- Градієнтами для акцентів
- Плавними анімаціями та переходами
- Іконками з React Icons
- Адаптивною версткою для всіх пристроїв

## 🔒 Безпека

- JWT токени для автентифікації
- Firebase Authentication
- Middleware для перевірки прав доступу
- Валідація даних на сервері
- Захист API endpoints

## 📝 Тестування

Для тестування системи:

1. Зареєструйте тестовий акаунт
2. Перший зареєстрований користувач отримує роль admin
3. Додайте медикаменти через адмін панель
4. Протестуйте процес замовлення

## 🤝 Підтримка

Якщо виникли питання або проблеми:

1. Перевірте файли конфігурації
2. Переконайтеся, що Firebase правильно налаштований
3. Перевірте консоль на наявність помилок

## 🛠️ Усунення поширених проблем (Troubleshooting)

1. Cloud Firestore API неактивний / PERMISSION_DENIED

Якщо у логах бачите:

```
Cloud Firestore API has not been used in project apteka-ee589 before or it is disabled
```

Приведіть Firestore API в дію:

- Перейдіть в Google Cloud Console -> APIs & Services -> Library -> знайдіть **Cloud Firestore API** та натисніть **Enable**.
- Або використайте gcloud:
  ```powershell
  gcloud services enable firestore.googleapis.com --project=apteka-ee589
  ```
- Після ввімкнення зачекайте кілька хвилин і перезапустіть сервер.

4. Скачування реальних зображень медикаментів (опціонально)

- Якщо ви хочете, щоб медикаменти отримали «реалістичні» фотографії (Wikimedia / Unsplash): створіть або редагуйте файл `server/seed/med_images.json` у форматі:

```json
{
  "Парацетамол 500 мг": "https://commons.wikimedia.org/wiki/Special:FilePath/Paracetamol_650.jpg",
  "Аспірин 100 мг": "https://commons.wikimedia.org/wiki/Special:FilePath/Aspirin1.jpg",
  "__generic_tablets__": "https://images.unsplash.com/photo-1580587771525-78b9dba3b914?auto=format&fit=crop&w=400&q=80"
}
```

- Для автоматичного завантаження цих фото виконайте:

```powershell
npm run seed -- --force --download-images
```

- Якщо якийсь медикамент не має конкретного фото у `med_images.json`, seed спробує автоматично знайти фото на Wikimedia Commons (автопоиск) і завантажити його; якщо не знайде — використає локальне `tablets.jpg` або Unsplash fallback.

- Будь ласка, перевіряйте ліцензії зображень на Wikimedia чи Unsplash — ці сервіси мають різні правила атрибуції (Wikimedia часто публічне домен або CC BY/SA; Unsplash вільний for many uses but check license).

2. Помилка реєстрації: `auth/email-already-exists`

Якщо при реєстрації ви отримали повідомлення про те, що email вже використовується:

- Використайте інший тестовий email.
- Видаліть тестового користувача в Firebase Console -> Authentication -> Users.
- У коді вже додана перевірка існування користувача перед створенням (на маршруті `/api/auth/register`).

3. DeprecationWarning: `util._extend` (Node.js)

Якщо у логах є попередження про застарілий API `util._extend`:

- Оновіть залежності проекту (`npm outdated` -> `npm update`).
- Оновіть `webpack`/`react-scripts` до свіжих версій, якщо використовуєте старі.
- Якщо попередження походить з пакетів сторонніх розробників, перевірте репозиторії пакетів або зафіксуйте оновлення у `package.json`.

4. Інші проблеми з правами доступу

Переконайтеся, що Service Account має достатні права:

- У Firebase Console -> Project Settings -> Service accounts
- Дайте Service Account права Firebase Admin або інші налаштовані права (не давайте Owner на продакшні без потреби).
